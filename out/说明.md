# out/ 迁移说明

由 `凛.json`(预设)中的 COT 规则拆分生成,时间换算规则已剔除(代码负责,见 `docs/agent-design.md` §3.5)。

## 内容去向

| 原预设条目/段落 | 去向 |
|---|---|
| DS思维链 → Admin: 后台推演模块 | backstage.txt(第 1-7 条) |
| DS思维链 → 值班在场映射 / 乱入检定 / 点名回路 | backstage.txt(第 8-10 条) |
| 🏖️人物关系细致分析 / 🏖️群像位置推演 | backstage.txt(第 11-12 条) |
| DS思维链 → 世界状态(事实核验/唯一可用素材) | backstage.txt(第 13 条) |
| DS思维链 → Exploration_Rules(并行场景) | parallel.txt |
| DS思维链 → Admin: 元叙事 | writer.txt(一) |
| DS思维链 → 正文生成·当下切片 / 多窗口 / 首次出场 / 记忆 / 事实纪律 / 规则 / 日常任务 / 输出格式 | writer.txt(二~十) |
| DS特化(中文思考强制 + ProhibitManifest + EmotionManifest) | writer.txt(开头 + 十一) |
| DS思维链 → Present_Residents / Admin: 世界引力 | writer.txt(六) |

## 剔除的内容(代码接管,禁止再贴回提示词)

- 时间同步公式(剧情日期 = 真实日期 + 7 天等)→ builtins.parse_signal_and_time
- Δt 分级表(4-30 分钟 / 30 分钟-2 小时 / 2 小时+)→ 同 builtins,结论在 {{timeContext}}
- 场景存在时间的累计/重置计算 → 代码(阶段 3 起自持,当前从【当前状态信息】解析)
- 时段判定(夜间/早晨/白昼/空档/安息日)→ 同 builtins,结论在 {{timeContext}}.period
- 乱入豁免判定(22:00 后/私密/02:00-起床)→ 同 builtins,结论在 {{timeContext}}.rollExempt
- 宏:`{{date}}/{{time}}/{{weekday}}/{{lastUserMessage}}/{{setvar::…}}/{{getvar::…}}/{{乱入舰娘}}` 全部移除

## 使用时的配置改动

1. 三个文件覆盖到 `sillytavern-server-plugin/prompts/` 下同名文件
2. `agent.json` 两处小改:
   - parallel 段的 `inputs` 增加 `"timeContext"`(豁免判断用)
   - writer 段的 `required_inputs` 保持 `["signal", "backstage"]`
3. 凛.json 按设计文档 §6 迁移清单处理(禁用 COT 条目、新增 `{{乱入舰娘}}` 占位)

## 校验

生成后已 grep 校验:三个文件不含任何时间换算公式与酒馆宏。

